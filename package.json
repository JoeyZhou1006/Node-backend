{
  "name": "node-backend",
  "version": "1.0.0",
  "description": "a backend that handles the backend logic of the ios app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0"
  }
}
